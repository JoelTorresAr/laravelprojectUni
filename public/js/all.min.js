$(document).ready(function(e) {
    $(".nav-link").bind('DOMSubtreeModified', function() {
        var item1 = $('a .nav-link')
        var array = $(this).parent().parent().siblings().find(item1)
        const found = array.prevObject
        found.addClass('active')
    })
});